<%= form_for([@advantage, @advantage.second_reviews.build]) do |f| %>
  <% if @second_review.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@second_review.errors.count, "error") %> prohibited this second_review from being saved:</h2>

      <ul>
      <% @second_review.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, '¿La solución planteada para el reto es buena?' %><br>
    <%= f.number_field :q7 %>
  </div>
  <div class="field">
    <%= f.label :name, '¿La solución tiene diferenciadores relevantes?' %><br>
    <%= f.number_field :q8 %>
  </div>
  <div class="field">
    <%= f.label :name, '¿El video comunica de manera clara la solución y sus ventajas?' %><br>
    <%= f.number_field :q9 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
