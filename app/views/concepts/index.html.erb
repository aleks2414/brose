<p id="notice"><%= notice %></p>

<h1>Listing Concepts</h1>

<table>
  <thead>
    <tr>
      <th>Entrevistas</th>
      <th>Propuesta</th>
      <th>Boceto2</th>
      <th>Team</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @concepts.each do |concept| %>
      <tr>
        <td><%= concept.entrevistas %></td>
        <td><%= concept.propuesta %></td>
        <td><%= concept.boceto2 %></td>
        <td><%= concept.team %></td>
        <td><%= link_to 'Show', concept %></td>
        <td><%= link_to 'Edit', edit_concept_path(concept) %></td>
        <td><%= link_to 'Destroy', concept, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Concept', new_concept_path %>
